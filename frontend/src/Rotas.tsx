import {
    BrowserRouter,
    Routes,
    Route
} from "react-router-dom";

export const Rotas = () => {
    return (
        <BrowserRouter>
            <Routes>
                
               
            </Routes>
        </BrowserRouter>
    )
}